12.3
    此版本包括了GPIO口的初始化，系统时钟的初始化（系统时钟的初始化可能需要修改）；TIM1,TIM8定时器初始化配置，主要包括pwm输出以及定时器中断定时；adc初始化配置，adc模式采用注入规则模式，串口初始化配置，配置opamp，起到放大电路的作用，放大倍数待实际测，配置了外部中断，主要是用于霍尔检测学习；

12.6
    配置COMP以及DAC，其中DAC输出电压待确定，COMP作用主要是用来保护电路，当超过DAC设定值时，触发定时器brake.(COMP的一端是接在Opamp的P结处，所以再设定DAC设定值时，应该根据板子最大电压除以放大倍数)，在实际的测试中，DAC电压设置为1V,当输入的电压为0.8V时，软件就会触发BRAKE，后续待新板过来后进行测试。其中由于B02A11-H1板子只有一个灯，DisplayErrLed函数需要进行修改，
    配置了Flash，实现的功能包括flash的读写操作，对窗口开门狗进行设置。
    程序芯片为STM32G474，后续进行修改


Problem:COMP2触发TIM8 brake现象：当只有PA7与正极连接，能正常触发Brake；但是当PA7与正极连接，PA5与负极连接时，就一直触发Brake，无法输出正常PWM。而COMP1触发TIM1 brake均能正常工作




can,tim,串口均测试完成版本
2022/1/8
电流采集均可以实现，过压欠压均添加
2022/1/10
增加电流限制，相序错误，霍尔错误，堵转保护。

2022/1/14
增加缺项保护，失速保护，修改flash，使其可以掉电后可以保存错误代码。

2022.1.21 CANopen调试通过 
1.修改CANOPEN协议栈内容：远程帧标志2----> 0x20000000，否则远程帧失效
2.CANFD库函数:（1）接收函数HAL_FDCAN_GetRxMessage增加DLC>>16U 去掉数据DLC>>16U
                        （2）发送函数FDCAN_CopyMessageToRAM：去掉DLC>>16U
FDcan中接收FIFO比普通CAN接收FIFO大，导致DLC与CANopen协议栈不兼容
3.修改CanRxMsgTypeDef  CanTxMsgTypeDef 并将该类型的指针指向 CANOPEN协议栈当中的CANTxBuf和CANRxBuf
FDcan与普通can发送消息结构体不同，为了兼容CANopen协议栈所以修改FDcan的数据发送与接收结构体（注意当中的定义的顺序，会影响协议栈中的MSG_DATA_INITIALIZER该宏定义）
测试：心跳、NMT远程帧、SDO发送和接收（1ms）正常未丢帧. TODO:TPDO和RPDO未测试以及断线保护和错误主动上报功能

2022.4.26
1.ADC采样改成上升沿和下降沿触发

2022.4.29
1.处理ADC采样点问题，已解决
2.处理ADC采样值问题，结果为电容有问题，更换后就可以正常采样到过零点

2022.5.10
1.处理自启动过程电机转动不平稳，过零点消失等问题，所更改的地方有无感启动程序与换向时间的频率不一样，目前都放在定时器中断服务函数中。另外对于
过零点丢失等情况，可以计数前后两次的换向时间，进行比较，大于一定差值后进行处理，目前是两个相差100就将新的值给覆盖。第三是自启动过程的判断，之前是通过自启动中的计数大于平均数，现在采取的是大于正要换向的换向时间。